{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "description": "Background worker: consumes RabbitMQ order-created messages, sends email/invoice (stubbed)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "amqplib": "^2.0.1"
  }
}
