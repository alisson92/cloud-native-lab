{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "Orders/catalog API: owns PostgreSQL (orders) and Redis (catalog cache)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@confluentinc/kafka-javascript": "^1.10.0",
    "amqplib": "^2.0.1",
    "express": "^5.2.1",
    "pg": "^8.22.0",
    "redis": "^6.2.0"
  }
}
