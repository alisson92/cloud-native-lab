name: bff-ci

on:
  push:
    branches: [main]
    paths: ["apps/bff/**"]
  pull_request:
    paths: ["apps/bff/**"]

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service: bff
      path: apps/bff
